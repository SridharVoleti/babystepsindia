export type SignupInput={email:string;password:string;confirmPassword:string;acceptedTerms:boolean;acceptedPrivacy:boolean};
export function normalizeEmail(value:string){const email=value.trim().toLowerCase();return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)?email:null;}
export function passwordError(password:string){if(password.length<12)return "Password must contain at least 12 characters.";if(!/[A-Z]/.test(password)||!/[a-z]/.test(password)||!/[0-9]/.test(password))return "Password must include upper-case, lower-case, and number characters.";return null;}
export function validateSignup(input:SignupInput):{ok:true;email:string}|{ok:false;error:string}{const email=normalizeEmail(input.email);if(!email)return {ok:false,error:"Enter a valid email address."};const error=passwordError(input.password);if(error)return {ok:false,error};if(input.password!==input.confirmPassword)return {ok:false,error:"Passwords do not match."};if(!input.acceptedTerms||!input.acceptedPrivacy)return {ok:false,error:"Accept the Terms and Privacy Policy to continue."};return {ok:true,email};}
