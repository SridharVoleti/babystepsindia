export type ParentProfile={id:string;account_status:"active"|"suspended"|"deleted";onboarding_status:"profile_pending"|"learner_pending"|"complete"};
export type ParentProfileStore={find(id:string):Promise<ParentProfile|null>;insert(id:string):Promise<ParentProfile>};
export async function ensureParentProfile(store:ParentProfileStore,userId:string){const existing=await store.find(userId);if(existing)return {profile:existing,created:false};return {profile:await store.insert(userId),created:true};}
export function parentAccessDecision(input:{emailVerified:boolean;profile:Pick<ParentProfile,"account_status">|null}){if(!input.emailVerified)return {allowed:false as const,code:"EMAIL_NOT_VERIFIED"};if(!input.profile)return {allowed:false as const,code:"PARENT_PROFILE_NOT_FOUND"};if(input.profile.account_status==="suspended")return {allowed:false as const,code:"ACCOUNT_SUSPENDED"};if(input.profile.account_status==="deleted")return {allowed:false as const,code:"ACCOUNT_DELETED"};return {allowed:true as const,code:null};}
